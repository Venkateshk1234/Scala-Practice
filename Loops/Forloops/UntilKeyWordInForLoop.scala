// Scala program to demonstrate 
// the until keyword in for loop

object UntilKeyWordInForLoop {
  def main(args: Array[String]) {  
        for( cnt <- 1 until 10 )
        {
            printf("%d ",cnt);
        }
        println()
    }
}